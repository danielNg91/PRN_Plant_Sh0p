@page
@model PlantShop.Pages.Orders.IndexModel
@{
    ViewData["Title"] = "Index";
}



<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="h2 inverse">Order List</h2>
        </div>
        <div class="col-6 text-right">
        </div>
    </div>

    <br /><br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    User
                </th>
                <th>
                    Total
                </th>
                <th>
                    Payment Status
                </th>
                <th>
                    Deliverty Status
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.Orders)
            {
                <tr>
                    <td width="10%">@obj.User.Fullname</td>
                    <td width="20%">@obj.Total</td>
                    <td width="20%">@obj.PaymentStatus</td>
                    <td width="20%">@obj.DeliveryStatus</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-page="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <a asp-page="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash-fill"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }

        </tbody>
    </table>

</div>
